namespace Athena.Application.Features.DashboardFeatures.Teachers.Spec
{
    /*public class TeachersByCourseSpec : Specification<TeacherCourseLevel>
    {
        public TeachersByCourseSpec(Guid couseId) =>
            Query.Where(e => e.CourseId == couseId)
                .Include(e => e.Teacher);
    }*/
}
